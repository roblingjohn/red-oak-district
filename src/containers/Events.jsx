import React from 'react';
import { Container } from 'react-bootstrap';

const Events = () => {
    return (
        <Container>
            The Events
        </Container>
    );
};

export default Events;